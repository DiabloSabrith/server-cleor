
# .dockerignore
node_modules
.git
.vscode
*.md
# dist ← УДАЛИТЬ эту строку, чтобы папка dist попала в образ
# .dockerignore
node_modules
.git
.vscode
*.md
# dist ← УДАЛИТЬ эту строку, чтобы папка dist попала в образ
# .dockerignore
node_modules
.git
.vscode
*.md
# dist ← УДАЛИТЬ эту строку, чтобы папка dist попала в образ
# .dockerignore
node_modules
.git
.vscode
*.md
# dist ← УДАЛИТЬ эту строку, чтобы папка dist попала в образ
# .dockerignore
node_modules
.git
.vscode
*.md
# dist ← УДАЛИТЬ эту строку, чтобы папка dist попала в образ